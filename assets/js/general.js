(function ($) {

    let initialExecution = true;

    function init()
    {
        //
    }

    function render()
    {
        //

        initialExecution = false;
    }

    $(document).render(render);
    $(document).ready(init);

}(jQuery));
